# UI Layer

Purely presentational components with no business logic.

## Components in this layer:
- Components that receive data via props
- Components that emit events via callbacks
- No direct state management (except local UI state)
- No side effects
- No business logic

## Examples:
- ODSCard - Pure presentational SDG card
- ScoreBreakdown - Pure presentational score display
- Toast - Pure presentational notification
